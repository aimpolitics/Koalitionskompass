# Beispiel für Streamlit Secrets
# Kopieren Sie diese Datei zu .streamlit/secrets.toml für lokale Entwicklung
# Für Streamlit Cloud, fügen Sie diese Werte in der Streamlit Cloud UI unter "Secrets" hinzu

# OpenAI API-Konfiguration 
[openai]
api_key = "sk-your-openai-api-key"

# Umgebungskonfiguration der App
[app]
environment = "production"  # Kann "development" oder "production" sein

# Pinecone Vektordatenbank-Konfiguration
[pinecone]
api_key = "your-pinecone-api-key"
environment = "your-pinecone-environment" # z.B. "gcp-starter" oder "us-west1-gcp"
index_name = "koalitionskompass"
namespace = "default"

# WICHTIG: Die hierarchische Struktur der Secrets mit Abschnitten ([openai], [pinecone])
# muss beibehalten werden, damit die Anwendung korrekt funktioniert. 